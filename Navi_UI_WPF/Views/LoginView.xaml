<Window
    x:Class="Navi_UI_WPF.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:Navi_UI_WPF.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Navi_UI_WPF.ViewModels"
    Title="NAVI System — Đăng Nhập"
    Width="440"
    Height="560"
    Background="#0D1B2A"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">

    <Window.Resources>
        <!-- Converters -->
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
        <conv:NullOrEmptyToVisibilityConverter x:Key="NullOrEmptyToVis" />

        <!-- TextBox style -->
        <Style x:Key="LoginFieldStyle" TargetType="Control">
            <Setter Property="Background" Value="#1A2A3C" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#2D4A6E" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Height" Value="44" />
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#42A5F5" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Login Button style -->
        <Style x:Key="LoginButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1565C0" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Height" Value="46" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Background="{TemplateBinding Background}"
                            CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1E88E5" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0D47A1" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#37474F" />
                                <Setter Property="Foreground" Value="#78909C" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Background gradient -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Offset="0" Color="#0D1B2A" />
                <GradientStop Offset="1" Color="#112240" />
            </LinearGradientBrush>
        </Grid.Background>

        <StackPanel
            Width="360"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">

            <!-- Logo + Title -->
            <TextBlock
                HorizontalAlignment="Center"
                FontSize="48"
                Text="⚙️" />
            <TextBlock
                Margin="0,8,0,4"
                HorizontalAlignment="Center"
                FontSize="28"
                FontWeight="Bold"
                Foreground="White"
                Text="NAVI SYSTEM" />
            <TextBlock
                Margin="0,0,0,32"
                HorizontalAlignment="Center"
                FontSize="13"
                Foreground="#64B5F6"
                Text="Production Assistant — Vui lòng đăng nhập" />

            <!-- User ID -->
            <TextBlock
                Margin="0,0,0,6"
                FontSize="13"
                FontWeight="SemiBold"
                Foreground="#90CAF9"
                Text="Mã nhân viên" />
            <TextBox
                x:Name="TxtUserId"
                Style="{StaticResource LoginFieldStyle}"
                Text="{Binding UserId, UpdateSourceTrigger=PropertyChanged}" />

        
            <!-- Error Message -->
            <Border
                Margin="0,12,0,0"
                Padding="12,8"
                Background="#B71C1C"
                CornerRadius="4"
                Visibility="{Binding ErrorMessage, Converter={StaticResource NullOrEmptyToVis}}">
                <TextBlock
                    FontSize="13"
                    Foreground="White"
                    Text="{Binding ErrorMessage}"
                    TextWrapping="Wrap" />
            </Border>

            <!-- Loading indicator -->
            <StackPanel
                Margin="0,12,0,0"
                HorizontalAlignment="Center"
                Orientation="Horizontal"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
                <TextBlock
                    FontSize="13"
                    Foreground="#64B5F6"
                    Text="⏳  Đang xác thực..." />
            </StackPanel>

            <!-- Login Button -->
            <Button
                Margin="0,20,0,0"
                Command="{Binding LoginCommand}"
                Content="Đăng Nhập"
                Style="{StaticResource LoginButtonStyle}" />

            <!-- Version -->
            <TextBlock
                Margin="0,24,0,0"
                HorizontalAlignment="Center"
                FontSize="11"
                Foreground="#455A64"
                Text="Navi UI v1.0 · LXA Production System" />
        </StackPanel>
    </Grid>
</Window>
