<UserControl
    x:Class="Navi_UI_WPF.Views.ProductAssemblyView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:Navi_UI_WPF.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:Navi_UI_WPF.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Navi_UI_WPF.ViewModels"
    d:DesignHeight="800"
    d:DesignWidth="1200"
    Background="#FAFAFA"
    mc:Ignorable="d"
    FocusManager.FocusedElement="{Binding ElementName=PoTextBox}"
    Loaded="UserControl_Loaded">
 

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="LocalBoolToVisibility" />
        <conv:NullOrEmptyToVisibilityConverter x:Key="NullOrEmptyToVisibility" />
        <conv:StepStatusToIconConverter x:Key="StepStatusToIcon" />

        <Style x:Key="AssemblyRowStyle" TargetType="DataGridRow" BasedOn="{StaticResource GridRow}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                    <Setter Property="Background" Value="#F1F8E9" />
                </DataTrigger>
                <DataTrigger Binding="{Binding IsNg}" Value="True">
                    <Setter Property="Background" Value="#FFEBEE" />
                </DataTrigger>
                <DataTrigger Binding="{Binding IsLocked}" Value="True">
                    <Setter Property="IsEnabled" Value="False" />
                    <Setter Property="Opacity" Value="0.6" />
                </DataTrigger>
                <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                    <Setter Property="FontWeight" Value="Bold" />
                    <Setter Property="Background" Value="#E3F2FD" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!--  Header  -->
            <RowDefinition Height="*" />
            <!--  Content  -->
        </Grid.RowDefinitions>

        <!--  Header  -->
        <Border
            Grid.Row="0"
            Padding="16,10"
            Background="{StaticResource BrandBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <!-- Form Title -->
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="20" />

                    <ColumnDefinition Width="Auto" />
                    <!--  PO Label  -->
                    <ColumnDefinition Width="200" />
                    <!--  PO TextBox  -->
                    <ColumnDefinition Width="Auto" />
                    <!--  PO Info Labels  -->
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <!--  Manufa Reference Info  -->
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <!--  Spacer  -->
                    <ColumnDefinition Width="Auto" />
                    <!--  Step Progress  -->
                </Grid.ColumnDefinitions>

                <!--  Form Title  -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock FontSize="20" FontWeight="Heavy" Foreground="White" Text="🛠 Assembly" VerticalAlignment="Center" />
                    <Border Width="1" Background="#50FFFFFF" Margin="20,0,0,0" Height="30" VerticalAlignment="Center" />
                </StackPanel>

                <!--  PO Label  -->
                <TextBlock
                    Grid.Column="2"
                    Margin="0,0,8,0"
                    VerticalAlignment="Center"
                    FontSize="14"
                    FontWeight="Bold"
                    Foreground="White"
                    Text="PO" />

                <!--  PO Input TextBox  -->
                <hc:TextBox
                    x:Name="PoTextBox"
                    Grid.Column="3"
                    VerticalAlignment="Center"
                    hc:InfoElement.Placeholder="Nhập số PO..."
                    FontSize="13"
                    Text="{Binding PoNumber, UpdateSourceTrigger=PropertyChanged}">
                    <hc:TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding SearchPoCommand}" />
                    </hc:TextBox.InputBindings>
                </hc:TextBox>

                <!--  PO Info: số PO + tên hàng  -->
                <StackPanel
                    Grid.Column="4"
                    Margin="0,0,0,0"
                    VerticalAlignment="Center">
                    <!--<TextBlock
                        FontSize="10"
                        Foreground="#90CAF9"
                        Text="Số PO " />
                    <TextBlock
                        FontSize="14"
                        FontWeight="Bold"
                        Foreground="White"
                        Text="{Binding PoNumber}" />-->
                    <!--<TextBlock
                    Margin="0,4,0,0"
                    FontSize="10"
                    Foreground="#90CAF9"
                    Text="Tên hàng" />
                    <TextBlock
                    FontSize="14"
                    FontWeight="SemiBold"
                    Foreground="White"
                    Text="{Binding ProductName}" />-->
                </StackPanel>

                <!--  Manufa Reference Info  -->
                <StackPanel
                    Grid.Column="6"
                    VerticalAlignment="Center"
                    Margin="30 0 30 0"
                    Visibility="{Binding Phtx, Converter={StaticResource NullOrEmptyToVisibility}}">
                    <TextBlock
                        FontSize="10"
                        Foreground="#90CAF9"
                        Text="Tên hàng" />
                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                        <TextBlock
                            FontSize="14"
                            FontWeight="Bold"
                            Foreground="White"
                            Text="{Binding Phtx}" />
                        <TextBlock
                            FontSize="14"
                            Foreground="#B3E5FC"
                            Text=" — "
                            Visibility="{Binding Phcd, Converter={StaticResource NullOrEmptyToVisibility}}" />
                        <TextBlock
                            FontSize="14"
                            FontWeight="Bold"
                            Foreground="White"
                            Text="{Binding Phcd}" />
                    </StackPanel>
                </StackPanel>

                <!--  Step Progress  -->
                <TextBlock
                    Grid.Column="8"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Right"
                    FontSize="14"
                    Margin="10 0 0 0"
                    Foreground="#E3F2FD">
                    <Run Text="Bước " />
                    <Run FontWeight="Bold" Text="{Binding CurrentStepNumber, Mode=OneWay}" />
                    <Run Text=" / " />
                    <Run Text="{Binding TotalSteps, Mode=OneWay}" />
                </TextBlock>
            </Grid>
        </Border>

        <!--  Loading Overlay  -->


        <!--  Main Content  -->
        <Grid Grid.Row="1" Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*" MinWidth="250" />
                <!--  Step List LEFT  -->
                <ColumnDefinition Width="Auto" />
                <!--  Splitter  -->
                <ColumnDefinition Width="4*" MinWidth="500" />
                <!--  Step Detail RIGHT  -->
            </Grid.ColumnDefinitions>

            <!--  Grid Splitter  -->
            <GridSplitter
                Grid.Column="1"
                Width="6"
                HorizontalAlignment="Center"
                VerticalAlignment="Stretch"
                Background="Transparent"
                ShowsPreview="True" />

            <!--  ══ Step List (LEFT) ═════════════════════════════════════  -->
            <Border
                Grid.Column="0"
                Padding="12"
                Background="White"
                CornerRadius="8"
                Effect="{StaticResource CardShadow}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <!--  Title  -->
                        <RowDefinition Height="Auto" />
                        <!--  SearchBar  -->
                        <RowDefinition Height="*" />
                        <!--  DataGrid  -->
                        <RowDefinition Height="Auto" />
                        <!--  Footer Nav  -->
                    </Grid.RowDefinitions>

                    <!--  Title  -->
                    <TextBlock
                        Grid.Row="0"
                        Margin="0,0,0,8"
                        FontSize="12"
                        FontWeight="SemiBold"
                        Foreground="{StaticResource TextMutedBrush}"
                        Text="DANH SÁCH BƯỚC" />

                    <!--  Search  -->
                    <hc:SearchBar
                        Grid.Row="1"
                        Height="32"
                        Margin="0,0,0,10"
                        hc:InfoElement.Placeholder="Tìm kiếm bước..."
                        Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

                    <!--  DataGrid  -->
                    <DataGrid
                        x:Name="StepsDataGrid"
                        SelectionChanged="StepsDataGrid_SelectionChanged"
                        Grid.Row="2"
                        AutoGenerateColumns="False"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1"
                        ColumnHeaderStyle="{StaticResource GridHeader}"
                        IsReadOnly="True"
                        CanUserSortColumns="False"
                        CanUserReorderColumns="False"
                        ItemsSource="{Binding StepsView}"
                        RowStyle="{StaticResource AssemblyRowStyle}"
                        SelectedItem="{Binding CurrentStep, Mode=TwoWay}"
                        Style="{StaticResource CompactGrid}">
                        <DataGrid.Columns>
                            <!--  Step Badge Column  -->
                            <DataGridTemplateColumn Width="55" Header="BƯỚC">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border
                                            Width="26"
                                            Height="26"
                                            HorizontalAlignment="Center"
                                            CornerRadius="13">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#BDBDBD" />
                                                    <Style.Triggers>
                                                        <!-- 1. OK Step: Light Green Background -->
                                                        <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                            <Setter Property="Background" Value="#C8E6C9" />
                                                        </DataTrigger>

                                                        <!-- 2. NG Step: Light Pink Background -->
                                                        <DataTrigger Binding="{Binding IsNg}" Value="True">
                                                            <Setter Property="Background" Value="#FFCDD2" />
                                                            <Setter Property="BorderBrush" Value="#C62828" />
                                                            <Setter Property="BorderThickness" Value="1" />
                                                        </DataTrigger>

                                                        <!-- 3. Current Step: Blue Background (Highest Priority) -->
                                                        <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                                            <Setter Property="Background" Value="{StaticResource BrandBrush}" />
                                                            <Setter Property="BorderThickness" Value="0" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                FontSize="11"
                                                FontWeight="Bold"
                                                Text="{Binding StepNumber}">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="White" />
                                                        <Style.Triggers>
                                                            <!-- Status Colors -->
                                                            <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                                <Setter Property="Foreground" Value="#2E7D32" />
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding IsNg}" Value="True">
                                                                <Setter Property="Foreground" Value="#C62828" />
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                                                <Setter Property="Foreground" Value="White" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!--  Description Column  -->
                            <DataGridTextColumn
                                Width="*"
                                Binding="{Binding Description}"
                                ElementStyle="{StaticResource CellText}"
                                Header="MÔ TẢ" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <!--  Footer Navigation (Removed Previous/Next buttons)  -->
                    <Grid Grid.Row="3" Margin="0,10,0,0" />

                </Grid>
            </Border>

            <!--  ══ Step Detail (RIGHT) ══════════════════════════════════  -->
            <Border
                Grid.Column="2"
                Background="White"
                CornerRadius="8"
                Effect="{StaticResource CardShadow}"
                Visibility="{Binding HasCurrentStep, Converter={StaticResource LocalBoolToVisibility}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <!--  Step Header  -->
                        <RowDefinition Height="5*" />
                        <!--  Image Area (Priority)  -->
                        <RowDefinition Height="Auto" MaxHeight="250" />
                        <!--  Detail Info (Compact)  -->
                    </Grid.RowDefinitions>

                    <!--  Step Header  -->
                    <Grid Grid.Row="0" Margin="16,12,16,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" VerticalAlignment="Center">
                            <Run
                                FontSize="18"
                                FontWeight="Bold"
                                Foreground="{StaticResource BrandBrush}"
                                Text="Bước " />
                            <Run
                                FontSize="18"
                                FontWeight="Bold"
                                Foreground="{StaticResource BrandBrush}"
                                Text="{Binding CurrentStepNumber, StringFormat={}{0}, Mode=OneWay}" />
                            <Run
                                FontSize="14"
                                Foreground="{StaticResource TextMutedBrush}"
                                Text=" — " />
                            <Run
                                FontSize="15"
                                Foreground="{StaticResource TextSecondaryBrush}"
                                Text="{Binding CurrentStep.Description}" />
                        </TextBlock>
                        <!--<CheckBox
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        Content="✓ Hoàn thành"
                        FontSize="13"
                        IsChecked="{Binding CurrentStep.IsCompleted}" />-->
                    </Grid>

                    <Rectangle
                        Grid.Row="0"
                        Grid.RowSpan="2"
                        Height="1"
                        Margin="16,0,16,0"
                        VerticalAlignment="Bottom"
                        Fill="{StaticResource BorderBrush}" />

                    <Grid Grid.Row="1" Margin="16,10,16,10">
                        <!--  Actual Image  -->
                        <Border
                            Background="#F5F5F5"
                            ClipToBounds="True"
                            CornerRadius="6"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            Visibility="{Binding CurrentStep.HasImages, Converter={StaticResource LocalBoolToVisibility}}">
                            <Image
                                RenderOptions.BitmapScalingMode="HighQuality"
                                Source="{Binding CurrentStep.Images, TargetNullValue={x:Null}}"
                                Stretch="Uniform" />
                        </Border>

                        <!--  Placeholder when no image  -->
                        <Border
                            Background="#F0F0F0"
                            CornerRadius="6"
                            Visibility="{Binding CurrentStep.HasImages, Converter={StaticResource LocalBoolToVisibility}, ConverterParameter=Invert}">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock
                                    HorizontalAlignment="Center"
                                    FontSize="40"
                                    Foreground="#BDBDBD"
                                    Text="📷" />
                                <TextBlock
                                    Margin="0,8,0,0"
                                    HorizontalAlignment="Center"
                                    FontSize="13"
                                    Foreground="#9E9E9E"
                                    Text="Hình ảnh minh họa" />
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!--  ROW 2: Chi tiết công đoạn  -->
                    <Border
                        Grid.Row="2"
                        Margin="0,8,0,0"
                        Padding="16,12,16,12"
                        BorderBrush="{StaticResource BorderBrush}" 
                        BorderThickness="0,1,0,0">
                            <Grid Visibility="{Binding CurrentStep.RequiresConfirmation, Converter={StaticResource LocalBoolToVisibility}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="16" />
                                    <ColumnDefinition Width="auto" />
                                </Grid.ColumnDefinitions>

                                <!-- Left: Info -->
                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <!-- Manufa PO Comments -->
                                    <Border 
                                        Margin="0,0,0,8"
                                        Padding="12,8"
                                        Background="#E8F5E9"
                                        BorderBrush="#A5D6A7"
                                        BorderThickness="0,0,0,3"
                                        CornerRadius="6"
                                        Visibility="{Binding ManufaComments, Converter={StaticResource NullOrEmptyToVisibility}}">
                                        <StackPanel>
                                            <TextBlock
                                                Margin="0,0,0,4"
                                                FontSize="11"
                                                FontWeight="Bold"
                                                Foreground="#2E7D32"
                                                Text="📋 GHI CHÚ TỪ MANUFA (PO)" />
                                            <TextBlock
                                                FontSize="13"
                                                Foreground="#1B5E20"
                                                LineHeight="18"
                                                Text="{Binding ManufaComments}"
                                                TextWrapping="Wrap" />
                                        </StackPanel>
                                    </Border>

                                    <!-- Technical Details -->
                                    <Border 
                                        Padding="12,8"
                                        Background="#FAFAFA"
                                        BorderBrush="{StaticResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,4" Visibility="{Binding CurrentStep.HasNote, Converter={StaticResource LocalBoolToVisibility}}">
                                                <TextBlock Text="※ CHÚ Ý: " FontWeight="Bold" Foreground="#E65100" FontSize="12" />
                                                <TextBlock Text="{Binding CurrentStep.Note}" FontSize="12" Foreground="#BF360C" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <WrapPanel Orientation="Horizontal">
                                                <StackPanel Orientation="Horizontal" Margin="0,0,16,0" Visibility="{Binding CurrentStep.HasBolts, Converter={StaticResource LocalBoolToVisibility}}">
                                                    <TextBlock Text="🔩 ỐC VÍT: " FontWeight="Bold" Foreground="#1976D2" FontSize="12" />
                                                    <TextBlock Text="{Binding CurrentStep.Bolts}" FontSize="12" FontWeight="SemiBold" Foreground="#0D47A1" />
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" Visibility="{Binding CurrentStep.HasForce, Converter={StaticResource LocalBoolToVisibility}}">
                                                    <TextBlock Text="⚡ LỰC SIẾT: " FontWeight="Bold" Foreground="#F57C00" FontSize="12" />
                                                    <TextBlock Text="{Binding CurrentStep.Force}" FontSize="12" FontWeight="SemiBold" Foreground="#E65100" />
                                                </StackPanel>
                                            </WrapPanel>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>

                                <!-- Right: Actions -->
                                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                 
                                    <Button Height="46"
                                            Background="#F44336"
                                            Foreground="White"
                                            BorderBrush="#B71C1C"
                                            BorderThickness="0,0,0,4"
                                            Cursor="Hand"
                                              Margin="0,0,0,8"
                                            Command="{Binding ConfirmNgCommand}"
                                            hc:BorderElement.CornerRadius="8">
                                        <Button.Effect>
                                            <DropShadowEffect BlurRadius="8" Direction="270" Opacity="0.2" ShadowDepth="2" />
                                        </Button.Effect>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock FontSize="20" Text="✖ " VerticalAlignment="Center" FontWeight="Black" />
                                            <TextBlock FontSize="16" FontWeight="Black" Text="NG" VerticalAlignment="Center" />
                                        </StackPanel>
                                    </Button>
                                       <Button Height="46"
                                            Background="#4CAF50"
                                            Foreground="White"
                                            BorderBrush="#1B5E20"
                                            BorderThickness="0,0,0,4"
                                            Cursor="Hand"
                                            Command="{Binding ConfirmOkCommand}"
                                            hc:BorderElement.CornerRadius="8">
                                        <Button.Effect>
                                            <DropShadowEffect BlurRadius="8" Direction="270" Opacity="0.2" ShadowDepth="2" />
                                        </Button.Effect>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock FontSize="20" Text="✔ " VerticalAlignment="Center" FontWeight="Black" />
                                            <TextBlock FontSize="16" FontWeight="Black" Text="OK" VerticalAlignment="Center" />
                                        </StackPanel>
                                    </Button>

                                </StackPanel>
                            </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
